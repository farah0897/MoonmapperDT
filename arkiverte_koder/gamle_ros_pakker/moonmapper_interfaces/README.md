# moonmapper_interfaces (arkivert)

**Arkivert:** 2026-05-17 sammen med `moonmapper_perception` og `moonmapper_ml`.

Meldinger: `TriadRaw`, `TriadFeatures`, `MaterialClassification` — planlagt for fysisk Triad + RF-pipeline (aldri ferdig koblet).

Offline ML bruker CSV + `scripts/predict.py`, ikke disse meldingene.
