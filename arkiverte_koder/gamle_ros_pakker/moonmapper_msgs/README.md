# moonmapper_msgs (arkivert)

**Arkivert:** 2026-05-17 sammen med `moonmapper_gz_sensors`.

Melding: `TriadSpectrum.msg` — brukt kun av Gazebo TriadSpectroscopy-plugin.

Fysisk robot + ML bruker `ml/` og `scripts/predict.py` (`moonmapper_interfaces` er også arkivert).
