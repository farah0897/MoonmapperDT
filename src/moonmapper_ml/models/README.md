# Runtime modeller (ROS2)

Denne mappen er for **små** modell-artefakter som skal brukes av `moonmapper_ml` i ROS2 runtime.

Anbefaling:
- Ikke commit store modeller til git.
- For utvikling kan du legge en liten `.joblib` her lokalt, og holde den utenfor git via `.gitignore`.

