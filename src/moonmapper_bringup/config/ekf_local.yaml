# robot_localization — lokal EKF: hjul-odometri + IMU -> /odometry/local, TF odom -> base_footprint
# Startes med node-navn ekf_local og remap av odometry/filtered -> /odometry/local

ekf_local:
  ros__parameters:
    frequency: 20.0
    sensor_timeout: 0.25
    two_d_mode: true
    use_sim_time: true
    publish_tf: true
    map_frame: map
    odom_frame: odom
    base_link_frame: base_footprint
    world_frame: odom

    odom0: /diff_drive_controller/odom
    odom0_config: [true, true, false,
                   false, false, false,
                   true, false, false,
                   false, false, true,
                   false, false, false]
    odom0_differential: false
    odom0_relative: false

    imu0: /imu/data
    imu0_config: [false, false, false,
                  false, false, true,
                  false, false, false,
                  false, false, true,
                  false, false, false]
    imu0_differential: false
    imu0_relative: false
    imu0_remove_gravitational_acceleration: true
